module.exports = () => ({
    "users-permissions": {
        config: {
        register: {
            // put the name of your added fields here
            allowedFields: ["firstName", "firstName", "phone"],
        },
        },
    }
});
